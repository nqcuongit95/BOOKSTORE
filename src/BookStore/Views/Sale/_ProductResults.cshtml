@model List<ProductFilterViewModel>


@foreach (var product in Model)
{
    <tr>
        <td>@product.Id</td>
        <td>@product.Name</td>
        <td>@product.Price</td>
        <td>@product.Available</td>
    </tr>
}