@model Notification

<div class="header"> @Model.Title </div>
<div class="content"> @Model.Content </div>
<div class="actions">
    <div class="ui blue labeled icon cancel button">
        <i class="checkmark icon"></i> @Model.Button
    </div>
</div>
